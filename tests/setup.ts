// Every test file runs after this. The environment itself is prepared in
// tests/env.ts, which global setup imports too, because it runs in a separate
// process and needs the same variables.
import './env';
