# eslint-config-next pulls its plugins in as transitive dependencies rather
# than declaring them as peers, and pnpm's strict node_modules layout means
# ESLint cannot resolve them from the project root. Hoisting the eslint
# packages is the documented pnpm workaround.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=@typescript-eslint/*
